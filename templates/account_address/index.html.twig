{% extends 'base.html.twig' %}

{% block title %}Mes adresses{% endblock %}

{% block body %}


    <section id="road">
        <div class="adresses">
            <div class="container">
                <a href="" class="btn btn-info float-end">Ajouter une adresse</a>
                <h1>Mes adresses</h1>
                <p>
                    C'est dans cette page que je pourrais gérer toutes mes adresses. <br>
                    <a href="{{ path('app_account') }}">Retour</a>
                </p>
                <hr>
                {% if app.user.addresses|length == 0 %}
                    <p>
                        Vous n'avez pas encore d'adresse dans votre compte client. Pour ajouter une, veuillez
                        <a href="">cliquer ici</a>
                    </p>
                {% else %}
                    AFFICHER DES ADRESSES
                {% endif %}
            </div>
        </div>
    </section>




{% endblock %}
